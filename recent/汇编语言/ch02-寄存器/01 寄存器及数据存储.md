---
title: '01 寄存器及数据存储'
---

# 01 寄存器及数据存储

* 通用寄存器可以切分成两个独立的寄存器使用：
  * 如：`AX` 可以拆分为 `AH` 和 `AL`
  * 作用：
    * 兼容运算
* 内部工作寄存器：
  * 包括：
    * 指令指针 `IP`（`PC`）
    * 变址寄存器 `SI` `DI`
    * 指针寄存器 `SP` `BP`：
      * 指向堆栈中的数据
    * 段寄存器
    * ...
* `FLAG` 寄存器（PSW）：
  * 用于存储计算机状态
