---
title: '03 进程的内存映像'
---

# 03 进程的内存映像

内存的存储区域划分：
* 操作系统内核区：内核代码、内核数据结构等
	* 包括 PCB
* 用户栈（Stack）：各个函数的栈帧，包含局部变量、函数调用的相关信息
* 共享库的存储映射区：库函数的代码
* 堆（Heap）：手动分配内存的区域
* 读 / 写数据：全局变量、静态变量
* 只读代码 / 数据：程序指令、只读数据
* 未使用区
只读代码 / 数据区和读 / 写数据区在进程启动时确定大小，固定不变

