操作系统对内存的管理：
* 负责内存空间的分配与回收
* 需要提供某种技术从逻辑上对内存空间进行扩充
* 提供地址转换功能，负责程序的逻辑地址与物理地址的转换
* 提供内存保护功能，保护各进程在各自存储空间内运行，互不干扰

内存保护的方式：
* 设置上、下限寄存器，存放进程的上、下限地址，进程的指令访问某个地址时先检查是否越界
* 采用重定位寄存器（基址寄存器，存放进程的起始物理地址）和界地址寄存器（限长寄存器，存放进程的最大逻辑地址）

