运行 -> 阻塞：等待某事件发生
运行 -> 就绪：时间片用尽
就绪 -> 运行：被调度程序选中
阻塞 -> 就绪：等待的事件发生

线程的组织：
* 线程表（Thread Table）：多个 TCB 可以组织成一张表
	* 线程控制块（Thread Control Block，TCB）：
		* 线程切换时保存 / 恢复：
			* 线程标识符（TID）
			* 程序计数器（PC）：线程下一步执行指令的地址
			* 其他寄存器：线程运行的中间结果
			* 堆栈指针：函数调用信息、局部变量等
		* 线程运行状态
		* 优先级：线程调度、资源分配的参考

