* `Get-Process`：
	* 查询当前运行的进程
	* `-Name`：
		* 按名称查询
	* `-Id`：
		* 按 PID 查询
* `Start-Process`：
	* 启动进程
	* `-FilePath`：
		* 程序路径
	* `-ArgumentList`：
		* 指定参数
	* `-PassThru`：
		* 将进程对象输出到管道中
* `Stop-Process`：
	* 终止进程
	* `-Id`
	* `-Name`
