* `$PSVersionTable`：
	* `PSVersion`：
		* PS 版本
	* `PSEdition`：
		* `Desktop`（5.1）
		* `Core`（7）
	* `OS`：
		* 平台
* PS 5.1：
	* Windows 自带
	* 基于 .NET Framework
	* 默认编码：
		* ANSI
* PS 7：
	* 跨平台开源
	* 基于 .NET Core
	* 默认编码：
		* 无 BOM 的 UTF8
	* 链式执行：
		* `&&`
		* `||`
	* 三元运算符：
		* `?:`
		* `??`
