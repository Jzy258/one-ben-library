* `$LASTEXITCODE`：
	* 上一次调用外部程序的退出码
	* `0`：
		* 成功
	* 非 `0`：
		* 失败
* `$?`：
	* 上一个 cmdlet 是否成功的布尔值
	* 表示是否成功执行
* `try-catch`：
	* 捕获终止性错误
	* 配合 `-ErrorAction Stop` 将错误结果转换为终止行为
	* `catch` 语句中可以使用 `$_` 访问错误对象
