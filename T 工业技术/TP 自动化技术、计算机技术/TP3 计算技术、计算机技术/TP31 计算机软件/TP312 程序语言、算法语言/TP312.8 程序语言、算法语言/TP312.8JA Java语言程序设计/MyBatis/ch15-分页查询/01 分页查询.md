传入页码和页的大小：
```xml
<select id="selectPage" resultType="Employee" parameterType="map">  
    SELECT  
        eno, ename    
    FROM        
	    t_employee    
	LIMIT #{pageNo}, #{pageSize}
</select>
```

可以使用 PageHelper 插件

