---
title: '04 properties'
---

# 04 properties

`<properties>` 用于配置属性：
* 包含若干 `<property>`，以键值对的形式存在，具有 `name` 和 `value`
* 定义的各个属性可以在后文中以 `${键名}` 的形式调用
* 可以通过 `resource` 或 `url` 属性引用外部资源文件（如 `.properties` 文件）

