`mybatis-config.xml` 中，`<transactionManager>` 用于管理事务：
* `type` 属性指定了使用哪一种事务管理器：
	* `JDBC`：
		* MyBatis 使用原生的 JDBC 代码管理事务
	* `MANAGED`：
		* MyBatis 不负责事务的管理，交给其他容器处理

在调用 `SqlSessionFactory` 的 `openSession` 方法时传入 `boolean` 参数，可以设定是否 `autoCommit`

