Spring 对事务的管理底层方式基于 AOP 实现

事务管理器 API：
* `PlatformTransactionManager`：
	* 顶级接口
* `DataSourceTransactionManager`：
	* 支持 JdbcTemplate（Spring 内置的 JDBC API）、MyBatis、Hibernate 等事务管理
* `JtaTransactionManager`：
	* 支持分布式事务管理

