`war` 和 `war_exploded` 的区别：
* `war`：
	* 压缩的 Web 应用程序归档文件（Web Application Archive）
	* 以单一文件（`.war`）的形式部署到服务器
	* 体积较小
	* 适合生产环境部署
	* 部署时需要服务器解压
* `war_exploded`：
	* 已解压的 `war` 文件
	* 以目录结构形式部署到服务器
	* 可以直接访问其中的文件和目录
	* 适合开发环境
	* 修改文件后能够立即生效

