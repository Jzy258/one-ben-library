* 深克隆方式：
* 重写 `clone()` 方法
* 使用字节数组流
* 原理：将要克隆的 Java 对象写到内存中的字节数组中，再从内存中的字节数组中读取对象，读取到的对象就是一个深克隆
* 对象拷贝方式：
* 调用 `Object` 的 `clone()` 方法，默认是浅克隆，（深克隆需要重写 `clone()` 方法）
* 序列化和反序列化
* 使用 `ByteArrayInputStream` 和 `ByteArrayOutputStream`
