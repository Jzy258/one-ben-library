---
title: '03 封装'
---

# 03 封装

* 封装：将数据和方法加以包装，使之成为一个独立的实体，并且把它与外部对象隔离开来的机制
* 作用：
* 保证数据的安全
* 提高代码的复用性
* 属性私有化：禁止外部程序对该属性进行随意访问
* 用 `private` 关键字修饰，可使属性只能在本类中访问
* 对外提供公开的访问入口
* 访问一般分为读和改两种（Getter 和 Setter）
* 在Setter方法中编写拦截用的语句
* 由于就近原则，Setter 方法中的 `this.` 不能省略
