* `java.lang.Math`
* `PI`：圆周率
* `static Number abs(Number a)`：绝对值
* `static float|double ceil(float|double a)`：向上取整
* `static float|double floor(float|double a)`：向下取整
* `static int|long round(float|double a)`：取整，四舍五入
* `static Number max(Number a, Number b)`：取较大值
* `static Number min(Number a, Number b)`：取较小值
* `static double sqrt(double a)`：算术平方根
* `static double pow(double a, double b)`：求幂
* `static double random()`：生成范围为 \[0.0, 1.0) 的随机数
