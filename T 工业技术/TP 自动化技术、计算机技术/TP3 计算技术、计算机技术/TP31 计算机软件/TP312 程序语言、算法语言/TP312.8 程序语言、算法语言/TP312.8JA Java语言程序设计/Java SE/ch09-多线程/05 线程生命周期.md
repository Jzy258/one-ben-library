* 线程生命周期：从线程对象新建，到最终线程死亡的整个过程
* 线程生命周期包括七个重要阶段：
* 新建状态（NEW）：线程对象 `new` 出来时
* 就绪状态（RUNNABLE）：有权利抢夺 CPU 时间片
* 运行状态（RUNNABLE）：`run` 开始或继续执行
* 超时等待状态（TIMED_WAITING）：有时长限定
* 等待状态（WAITING）：无期限的等待
* 阻塞状态（BLOCKED）：遇到锁进入
* 死亡状态（TERMINATED）：`run` 方法执行完毕
