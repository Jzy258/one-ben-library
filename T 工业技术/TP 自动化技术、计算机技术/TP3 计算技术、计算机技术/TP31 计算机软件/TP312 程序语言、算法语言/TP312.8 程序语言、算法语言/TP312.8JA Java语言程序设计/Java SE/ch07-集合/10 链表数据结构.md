---
title: '10 链表数据结构'
---

# 10 链表数据结构

* 链表：
* 单向链表
* 双向链表
* 环形链表
	* 环形单链表
	* 环形双链表
* 链表由若干结点相连组成：
* 单向链表每个结点都存储了一个数据的内存地址和下一个结点的内存地址
* 双向链表每个结点比单向链表增加了上一个结点的内存地址
* 环形链表仍存在头结点和尾结点，只是尾（头）结点存储了头（尾）结点的内存地址而非空
* 链表的内存地址不是连续的，不涉及元素位移，故随机增删元素的效率较高，时间复杂度 $O(1)$
* 链表在查找时只能从某个结点开始逐个查找，查找效率较低，时间复杂度 $O(n)$
