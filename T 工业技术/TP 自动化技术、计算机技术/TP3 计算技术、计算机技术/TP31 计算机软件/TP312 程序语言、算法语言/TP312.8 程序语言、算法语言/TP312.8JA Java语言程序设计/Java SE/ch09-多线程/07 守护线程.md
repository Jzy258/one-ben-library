---
title: '07 守护线程'
---

# 07 守护线程

* 一种特殊的线程，当所有的非守护线程（用户线程）都结束时，守护线程会自动退出
* GC 线程就是一种典型的守护线程
* 创建守护线程的方法：
* 将线程的 `setDaemon()` 方法设置为 `true`
* 只有在启动守护线程之前将其设置为守护线程才有效
* 守护线程不是每次运行都会执行完成，不能依赖守护线程完成关键任务
* 作用：
* 辅助非守护线程
* 在特定场景下提高性能
* 守护线程一般都是死循环
