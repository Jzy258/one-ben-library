* `Duration`：
	* **粒度**：
		* s / ns
	* **适用于**：
		* 计算耗时
		* 超时判断
	* `Duration.between()`：
		* 计算两个时刻的间隔
	* `toXxxs`：
		* 计算合计（如合计小时）
* `Period`：
	* **粒度**：
		* y / M / d
	* **适用于**：
		* 计算年龄
		* 账单周期
	* `getXxxs`：
		* 只取该部分字段，不合计
