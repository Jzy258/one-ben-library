* ![[8.1.png]]
* 上图为常用 IO 流：
* `InputStream`：字节输入流
* `OutputStream`：字节输出流
* `Reader`：字符输入流
* `Writer`：字符输出流
* 以上 4 个流都是**抽象类**，是所有 IO 流的大类
* 所有的流都实现了 `Closeable` 接口，都有 `close()` 方法，流用完要关闭
* 所有的输出流都实现了 `Flushable` 接口，都有 `flush()` 方法，作用是将缓存清空，全部写出
