* `static`：静态的
* 静态变量：所有对象相同的属性
* 用 `类名.` 访问所有 `static` 修饰的变量、方法，不需要 `new` 对象
* 静态变量可以使用 `引用.变量名` 调用，但不建议
* 静态变量存储在堆内存中（Java 8 后），类加载时初始化
* 静态变量使用空引用调用不会产生空指针异常
* 静态方法中无法直接调用实例相关的数据
* 静态代码块位于类体中：
```java
static {
	Java语句;
}
```
* 静态代码块加载的时机：类加载时
