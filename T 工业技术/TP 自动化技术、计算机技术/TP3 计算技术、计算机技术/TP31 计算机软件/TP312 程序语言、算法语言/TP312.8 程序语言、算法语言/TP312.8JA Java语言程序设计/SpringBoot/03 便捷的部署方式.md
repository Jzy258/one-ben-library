> module `springboot-002`
* 使用 maven 插件打包部署 Spring Boot 项目
	* **流程**：
		* 引入 `spring-boot-maven-plugin` 插件
		* 执行 `mvn package` 命令打包
		* 执行 `java -jar xxx.jar`
			* 执行时会读取 jar 包外的配置文件
	* 打包所得 jar 包很臃肿