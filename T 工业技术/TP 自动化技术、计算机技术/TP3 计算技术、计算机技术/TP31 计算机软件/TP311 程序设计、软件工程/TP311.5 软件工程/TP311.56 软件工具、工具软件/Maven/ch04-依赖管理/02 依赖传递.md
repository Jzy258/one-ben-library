依赖传递：
* B 是 A 的直接依赖，C 是 B 的直接依赖，则 C 会作为间接依赖引入 A 中
* 依赖传递会受到依赖范围的影响

C 对 B 的范围是 `compile` 时，C 对 A 的依赖范围与 B 对 A 的范围一致

C 对 B 的范围是 `test` 或 `provided` 时，C 对 A 不可见

C 对 B 的范围是 `runtime` 时，C 对 A 的依赖范围与 B 对 A 的范围一致，除了 `compile` 对应 `runtime`

