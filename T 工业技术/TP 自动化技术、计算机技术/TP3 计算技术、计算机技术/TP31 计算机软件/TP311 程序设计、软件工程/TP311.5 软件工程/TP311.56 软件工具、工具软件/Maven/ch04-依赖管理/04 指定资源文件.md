项目中某些资源文件不存放在 `resources` 目录中，需要在 `pom.xml` 中指定，以便于在构建时包含这些资源文件

在 `<project>` -> `<build>` -> `<resources>` -> `<resource>` 中：
* `<directory>`：资源文件所在的目录（不一定是直接父目录）
* `<includes>`：包含若干 `<include>`，每一个 `<include>` 对应一个 `**/*.文件扩展名`

