---
title: '10 AJAX的异步与同步'
---

# 10 AJAX的异步与同步

异步：AJAX 请求互不干扰地执行，不存在先后执行问题

同步：AJAX 请求依次逐个执行

实现：
```js
xhr.open(method, url, async);
```
* 设置 `async` 属性，`true` 为异步，`false` 为同步

同步的使用场景：
* 大部分情况下使用异步
* 一个请求必须等待其他请求执行完后才能执行，则使用同步

