---
title: '08 props'
---

# 08 props

* **作用**：
	* 父组件向子组件传递数据
* 传递数据：
	* 直接在子组件标签上添加属性
	* 支持使用 `v-bind`
	* 子组件内使用 `defineProps` 函数接收
		* 参数传入字符串数组，数组元素对应传递的属性名
		* 返回值可以使用对象类型变量接收，作为该对象的属性
		* 为 `defineProps` 添加泛型以指定**返回值对象**的类型
	* 通过 `withDefaults` 指定默认值
