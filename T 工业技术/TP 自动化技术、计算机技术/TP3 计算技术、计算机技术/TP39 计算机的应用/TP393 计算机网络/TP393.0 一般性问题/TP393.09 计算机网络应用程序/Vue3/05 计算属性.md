* 计算属性（只读）：==component `User03.vue`==
	* 只要计算属性关联的任意属性值发生变化，则计算函数就会被调用一次
	* 若关联的属性值没有变化，则值会从缓存中获取
	* 默认情况下只读
* 计算属性（可读可写）：
	* 需要定义 setter 和 getter
	* 读取计算属性值时自动调用 getter
	* 修改计算属性值时自动调用 setter，并接收传入的参数
