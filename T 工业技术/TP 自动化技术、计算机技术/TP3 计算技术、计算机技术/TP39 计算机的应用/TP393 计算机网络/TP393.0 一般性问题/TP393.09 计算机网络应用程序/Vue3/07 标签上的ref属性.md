* `ref`：==component `User07.vue`==
	* 类似 DOM 中的 `id`
	* **解决问题**：
		* SPA 开发中不同组件内元素的 ID 冲突问题
	* `ref` 参数为空时，自动根据变量名寻找对应的普通标签的  `ref` 属性
	* 在页面加载完毕后才能通过 `ref` 获取元素
* `setup` 函数在组件创建时执行，只执行一次
* 在组件标签上添加 `ref` 可以获取组件实例，以访问组件内的属性
	* 需要使用 `defineExpose` 暴露组件内的属性
