* `el-radio`：
	* `v-model`：
		* 用于与变量双向绑定
		* 设为同一个值以将单选框分组
	* `value`：
		* 一个选项的标识符
	* `@change`：
		* 同一组选项内，选项选择改变事件
* `el-radio-group`：
	* 单选框组
	* 用于 `v-model` 和 `@change` 的复用
* `el-checkbox` 和 `el-checkbox-group` 同上
